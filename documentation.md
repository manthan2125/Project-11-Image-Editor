### accept="image/*" is an attribute used with the 
<input type="file">element. 
It tells the browser to allow the user to select only image files.

### Remix Icon  - for icons

### aspect-ratio: 4/3; -> (width/height)

